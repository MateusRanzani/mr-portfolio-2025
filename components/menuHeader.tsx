import { Button } from "./ui/button";

export default function MenuHeader() {
    return (
        <nav className="fixed top-4 left-1/2 transform -translate-x-1/2 w-[95vw] bg-red-800">
            <Button></Button>
        </nav>
    )
}
